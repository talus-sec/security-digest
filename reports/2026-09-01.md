# Security Digest — 2026-09-01

## Database

The single most urgent item this period is **CVE-2019-1068 in Microsoft SQL Server**, a CISA KEV-confirmed actively exploited vulnerability with an expired remediation due date of August 29, 2026. While several CVSS-critical vulnerabilities appear in the database landscape—including an ArcadeDB authentication bypass (CVSS 9.8) and hardcoded credential exposures in PostgreSQL tooling (CVSS 9.8)—CISA's confirmed exploitation classification takes precedence over theoretical severity scores. Immediate action is required for the SQL Server entry; the remaining entries below are sorted by confirmed-exploitation status first, then CVSS descending.

### Formal Vulnerabilities

#### MySQL — CVE-2026-55215 (CVSS 7.5 HIGH)
**Summary:** MariaDB Connector/Node.js (versions prior to 3.3.3, 3.4.6, 3.5.3) transmits database credentials to the server before completing TLS certificate validation when SSL is enabled without a pinned CA or server certificate. An active man-in-the-middle attacker can present a self-signed certificate, capture the plaintext password, and authenticate directly—even though the connector subsequently rejects the server and closes the connection. This affects any application using the Node.js connector with SSL enabled but no certificate pinning.

**Affected Features / Interoperability:** TLS/SSL handshake and authentication handshake in the MariaDB Connector/Node.js library. Downstream impact: any application using this connector to connect to MariaDB or MySQL with `ssl` enabled but without pinned certificates or strict CA validation.

**Source:** https://github.com/mariadb-corporation/mariadb-connector-nodejs/releases/tag/3.3.3

**Actions:**
- Fix: Upgrade to MariaDB Connector/Node.js 3.3.3, 3.4.6, or 3.5.3.
- Workaround: Enforce strict TLS certificate validation by pinning the server certificate or CA (`ca` option in connection string) and disabling fallback to insecure modes.
- Tradeoff: Requires obtaining and maintaining the server's TLS certificate or CA chain; certificate rotation requires connector config updates.

#### MySQL — CVE-2026-75133 (CVSS 7.5 HIGH)
**Summary:** The Keep Backup Daily WordPress plugin (versions prior to 2.1.4) exposes a cron endpoint (`kbd_cron_process`) without authentication, allowing unauthenticated attackers to trigger full MySQL database dumps. Attackers can predict the partially randomized backup filename based on the database name, a limited random range, and the Unix timestamp, then download the backup from the publicly accessible uploads directory.

**Affected Features / Interoperability:** The plugin's backup creation and storage mechanism; any WordPress instance using this plugin with MySQL as its backend database.

**Source:** https://www.vulncheck.com/advisories/keep-backup-daily-wordpress-plugin-sensitive-information-exposure-via-kbd-cron-process

**Actions:**
- Fix: Upgrade to Keep Backup Daily plugin version 2.1.4.
- Workaround: Restrict web access to the `wp-content/uploads/` directory and the plugin's cron endpoint via web server configuration (e.g., deny all in nginx/apache); disable the plugin if not actively needed.
- Tradeoff: Blocking uploads directory may break legitimate plugins that serve user content from that path.

#### MySQL — CVE-2026-39931 (CVSS 7.2 HIGH)
**Summary:** OpenEMR through version 8.2.0 contains an authenticated SQL injection vulnerability in the backup configuration import feature. Administrators with admin or super ACL privileges can upload a crafted SQL file at `form_step=202` in `backup.php`, and the application passes unfiltered input to `shell_exec` invoking the `mysql` CLI. Attackers can extract credential hashes, modify access controls, inject backdoor accounts, or write arbitrary files if MySQL FILE privileges and `secure_file_priv` settings permit.

**Affected Features / Interoperability:** OpenEMR's backup import functionality and MySQL CLI interaction; affects OpenEMR deployments using MySQL as the backend database.

**Source:** https://jivasecurity.com/writeups/openemr-backup-import-arbitrary-sql-cve-2026-39931

**Actions:**
- Fix: No specific patched version is cited in available data; monitor OpenEMR's security advisories for version 8.2.1 or later.
- Workaround: Restrict the `backup.php` import feature to super-admin accounts only; consider disabling the feature entirely if not required for operations; enforce least-privilege MySQL user for the application with FILE privileges revoked.
- Tradeoff: Disabling backup import may disrupt legitimate administrative workflows; restricting to super-admin limits delegation.

#### MySQL — CVE-2026-55855 (CVSS 6.5 MEDIUM)
**Summary:** MariaDB Connector/Node.js (versions prior to 3.2.4, 3.3.3, 3.4.6, 3.5.3) permits SQL injection when attacker-controlled Buffer parameters are escaped client-side under certain Asian character sets (big5, gbk, sjis, cp932, gb18030). The connector's byte-escaping logic bypasses charset-aware validation, allowing an attacker-controlled lead byte to consume a backslash as a multibyte trail byte and leave the following quote unescaped, terminating the string and enabling arbitrary SQL. The default `utf8mb4` charset and prepared statement parameters are not affected.

**Affected Features / Interoperability:** SQL parameter escaping under legacy Asian character sets in MariaDB Connector/Node.js; impacts applications that use these charsets and pass attacker-influenced data as parameters.

**Source:** https://github.com/mariadb-corporation/mariadb-connector-nodejs/releases/tag/3.2.4

**Actions:**
- Fix: Upgrade to MariaDB Connector/Node.js 3.2.4, 3.3.3, 3.4.6, or 3.5.3.
- Workaround: Avoid using affected character sets (big5, gbk, sjis, cp932, gb18030) when connecting; migrate to `utf8mb4`.
- Tradeoff: Migration from legacy charsets may require schema changes and application testing for encoding edge cases.

#### MySQL — CVE-2026-82553 (CVSS 6.3 MEDIUM)
**Summary:** The sambitraj Student Management System (up to commit `56ba287f2e9031523ccb4244cb6e3fe530e4e5d5`) has an improper authorization flaw in the `mysqli_query` function within `student_dashboard.php`. A remote attacker can manipulate the `roll_no` argument to bypass authorization checks. The project has a rolling release model with no disclosed version numbers and has not responded to the reported issue.

**Affected Features / Interoperability:** Student Dashboard component using `mysqli_query`; impacts any deployment of this system that uses MySQL as its backend.

**Source:** https://github.com/sambitraj/STUDENT-MANAGEMENT-SYSTEM/issues/6

**Actions:**
- Fix: Monitor the project's repository for security patches; no fixed version is currently documented.
- Workaround: Restrict access to `student_dashboard.php` via network-level controls or web server authentication; implement additional application-layer authorization checks.
- Tradeoff: May disrupt legitimate student-facing functionality; network restrictions may affect remote access scenarios.

#### MySQL — CVE-2026-55854 (CVSS 5.9 MEDIUM)
**Summary:** MariaDB Connector/Node.js (versions prior to 3.2.4, 3.3.3, 3.4.6, 3.5.3) can disclose an account password when PAM dialog authentication is negotiated over an insecure transport. With default `sslMode=DISABLE` and `restrictedAuth=null` settings, a hostile or on-path server can request dialog authentication over plain TCP, causing the connector to transmit the password in cleartext. Proper TLS verification or local Unix sockets prevent this path, but fingerprint-only validation is insufficient.

**Affected Features / Interoperability:** PAM (Pluggable Authentication Modules) authentication in MariaDB Connector/Node.js; impacts deployments using PAM auth with the default SSL disabled.

**Source:** https://github.com/mariadb-corporation/mariadb-connector-nodejs/releases/tag/3.2.4

**Actions:**
- Fix: Upgrade to MariaDB Connector/Node.js 3.2.4, 3.3.3, 3.4.6, or 3.5.3.
- Workaround: Enforce TLS for connections using PAM auth (`sslMode=VERIFY_CA` or `VERIFY_IDENTITY`); avoid `sslMode=DISABLE` with PAM.
- Tradeoff: TLS enforcement may require certificate infrastructure changes; additional latency from TLS handshake.

#### MySQL — CVE-2026-55856 (CVSS 5.9 MEDIUM)
**Summary:** MariaDB Connector/J (versions prior to 2.7.14, 3.3.5, 3.4.3, 3.5.9) can be susceptible to credential interception during the initial TLS handshake when connecting with `sslMode=verify-full` or `sslMode=verify-ca` and a password, but without configuring `serverSslCert` or a trustStore. The connector's initial-handshake path does not enforce certificate fingerprint validation, allowing an active man-in-the-middle to present a self-signed certificate, claim to be the database server, and receive the password via `mysql_clear_password` before the connection is rejected.

**Affected Features / Interoperability:** Initial TLS handshake and authentication in MariaDB Connector/J; impacts Java applications using these SSL modes without explicit certificate pinning.

**Source:** https://github.com/mariadb-corporation/mariadb-connector-j/security/advisories/GHSA-g9jj-cgmh-9f38

**Actions:**
- Fix: Upgrade to MariaDB Connector/J 2.7.14, 3.3.5, 3.4.3, or 3.5.9.
- Workaround: Configure `serverSslCert` or a trustStore containing the server's CA; enable `sslMode=VERIFY_IDENTITY` and ensure `restrictedAuth` is set appropriately.
- Tradeoff: Requires maintaining server certificates or CA chains; additional configuration complexity.

#### MySQL — CVE-2026-55857 (CVSS 5.9 MEDIUM)
**Summary:** MariaDB Connector/J (versions prior to 2.7.14, 3.3.5, 3.4.3, 3.5.9) allows PAM dialog authentication to transmit the account password over an insecure connection. The `mysql_clear_password` plugin is protected by secure-transport requirements, but the sibling PAM dialog handler does not inherit this protection, defaulting to no secure-transport requirement. With default `sslMode=DISABLE` and `restrictedAuth=null`, a hostile server can request dialog auth over plain TCP and receive the plaintext password.

**Affected Features / Interoperability:** PAM dialog authentication path in MariaDB Connector/J; impacts deployments using PAM auth without TLS enforcement.

**Source:** https://github.com/mariadb-corporation/mariadb-connector-j/security/advisories/GHSA-qxvw-fvwx-5cp7

**Actions:**
- Fix: Upgrade to MariaDB Connector/J 2.7.14, 3.3.5, 3.4.3, or 3.5.9.
- Workaround: Enforce TLS for connections using PAM auth; avoid `sslMode=DISABLE`.
- Tradeoff: Same as CVE-2026-55854—may require certificate infrastructure changes.

#### MySQL — CVE-2026-55858 (CVSS 5.9 MEDIUM)
**Summary:** MariaDB Connector/J (versions prior to 2.7.14, 3.3.5, 3.4.3, 3.5.9) encodes and decodes protocol text assuming the connection character set is UTF-8. The server can announce a mid-session change to `character_set_client` (e.g., via `SET NAMES`, stored routines, triggers, or a hostile server), causing the driver to continue reading/writing UTF-8 while the server interprets the same bytes under a different encoding. This leads to silent data corruption and can defeat byte-wise quoting or escaping. The fix rejects any non-UTF-8 charset after initialization with a SQLException.

**Affected Features / Interoperability:** Character set handling in MariaDB Connector/J protocol; impacts applications where server-side configuration or attacker-controlled sessions can alter the character set mid-session.

**Source:** https://github.com/mariadb-corporation/mariadb-connector-j/releases/tag/2.7.14

**Actions:**
- Fix: Upgrade to MariaDB Connector/J 2.7.14, 3.3.5, 3.4.3, or 3.5.9.
- Workaround: Enforce `character_set_client` at the server level via `init_connect` or configuration to prevent mid-session changes; avoid granting attackers the ability to execute arbitrary SQL.
- Tradeoff: Server-level restrictions may break legitimate applications that legitimately change character sets; driver upgrade is the cleaner fix.

#### MySQL — CVE-2026-55859 (CVSS 5.9 MEDIUM)
**Summary:** MariaDB Connector/R2DBC (versions prior to 1.4.1) has the same charset-confusion vulnerability as CVE-2026-55858, but in the R2DBC non-blocking driver for Java. A server can announce a mid-session character set change via OK-packet session-state tracking, causing silent data corruption when the driver continues using UTF-8 while the server interprets bytes under a different encoding. The fix rejects non-UTF-8 character sets after initialization with an exception.

**Affected Features / Interoperability:** Character set handling in the R2DBC driver for MariaDB and MySQL; impacts reactive/non-blocking Java applications using this driver.

**Source:** https://github.com/mariadb-corporation/mariadb-connector-r2dbc/security/advisories/GHSA-5rqc-86vf-g8r2

**Actions:**
- Fix: Upgrade to MariaDB Connector/R2DBC 1.4.1.
- Workaround: Prevent mid-session character set changes via server configuration or access controls as with CVE-2026-55858.
- Tradeoff: Same as CVE-2026-55858—driver upgrade is preferred.

---

#### PostgreSQL — CVE-2026-74891 (CVSS 9.8 CRITICAL)
**Summary:** `openssl_encrypt` versions prior to 1.4.0 contain hardcoded database credentials in standalone server configuration files. Attackers on the same network can use these well-known default credentials to access PostgreSQL databases directly and retrieve sensitive data. This is a pre-configured credential exposure rather than a运行时 vulnerability.

**Affected Features / Interoperability:** Standalone server configuration files in `openssl_encrypt`; impacts any PostgreSQL database deployed alongside or managed by affected versions of this software.

**Source:** https://github.com/jahlives/openssl_encrypt/security/advisories/GHSA-v4vm-4xf2-fhqj

**Actions:**
- Fix: Upgrade `openssl_encrypt` to version 1.4.0 or later.
- Workaround: Immediately rotate any credentials that may have been present in configuration files; audit access logs for unauthorized access using default credentials.
- Tradeoff: Credential rotation requires updating all dependent configurations; no functionality degradation.

#### PostgreSQL — CVE-2026-76635 (CVSS 7.2 HIGH)
**Summary:** baserCMS before version 5.3.0 has a SQL injection vulnerability in `BcDatabaseService.php` that allows authenticated administrators to inject attacker-controlled table names and configuration values directly into SQL statements. Attackers can chain this with a backup restore code injection flaw (PHP code outside class definitions in schema files executes unconditionally) to plant malicious table names and trigger error-based SQL injection against the PostgreSQL backend, retrieving database version, schema contents, and arbitrary data.

**Affected Features / Interoperability:** baserCMS database service layer; impacts any baserCMS deployment using PostgreSQL as its backend.

**Source:** https://github.com/baserproject/basercms/security/advisories/GHSA-cg65-f2m7-9fqj

**Actions:**
- Fix: Upgrade baserCMS to version 5.3.0.
- Workaround: Restrict administrator access to trusted users only; consider disabling the backup restore feature if not operationally critical.
- Tradeoff: Limiting administrator access may disrupt workflows; disabling backup restore limits recovery options.

#### PostgreSQL — CVE-2026-70635 (CVSS 7.1 HIGH)
**Summary:** TimescaleDB through version 2.29.1 has an out-of-bounds read vulnerability in bulk text dictionary decompression. Authenticated attackers with direct DML access to a non-frozen physical compressed hypertable can supply a crafted Simple8b selector-11 value that bypasses index validation, causing an out-of-bounds read before the base of the live offsets array. This can result in incorrect aggregation output, backend `SIGSEGV`, or PostgreSQL crash recovery depending on build configuration.

**Affected Features / Interoperability:** TimescaleDB compressed hypertable operations; impacts TimescaleDB deployments using physical compression on hypertables with the Arrow dictionary-index type.

**Source:** https://github.com/timescale/timescaledb/pull/10360

**Actions:**
- Fix: Upgrade TimescaleDB to the version containing commit `517c13e` (verify with vendor); monitor Timescale security advisories.
- Workaround: Avoid direct DML on compressed hypertables for untrusted users; disable the VectorAgg feature if not required; ensure `max_worker_processes` and recovery configurations are set to handle potential crashes.
- Tradeoff: Restricting DML on compressed hypertables may disrupt legitimate analytics workloads.

#### PostgreSQL — CVE-2026-75132 (CVSS 6.5 MEDIUM)
**Summary:** WAPT Server versions 2.6.1.17834 and earlier have a SQL injection vulnerability in the `columns` parameter of the `GET /api/v3/hosts` endpoint. A remote authenticated user with read-only privileges can inject arbitrary PostgreSQL expressions into the SQL query, bypass host scope restrictions, and read data from other rows or tables within the database.

**Affected Features / Interoperability:** WAPT Server API endpoint for host enumeration; impacts WAPT Server deployments using PostgreSQL as their backend.

**Source:** https://www.vulncheck.com/advisories/wapt-server-sql-injection-via-api-v3-hosts-endpoint

**Actions:**
- Fix: Upgrade WAPT Server per vendor changelog (verify version 2.6.1.17835 or later addresses this).
- Workaround: Restrict API access to the `/api/v3/hosts` endpoint via network controls; audit API access logs for anomalous `columns` parameter values.
- Tradeoff: Network restrictions may affect legitimate API consumers; logging may introduce storage overhead.

#### PostgreSQL — CVE-2026-76212 (CVSS 5.3 MEDIUM)
**Summary:** phpMyFAQ before version 4.1.7, when using PostgreSQL via the native `pgsql` PHP extension, declares an incorrect LIKE ESCAPE character ('=') in the backend while the escape function uses a '|' prefix. This makes wildcard escaping a no-op, leaving `%` and `_` characters active in user-supplied search input. An unauthenticated attacker can submit these characters in the public FAQ search form to force maximally broad pattern matches and expensive sequential scans, resulting in denial of service. The PDO PostgreSQL backend is unaffected, and this does not enable SQL injection.

**Affected Features / Interoperability:** phpMyFAQ search functionality when using the native `pgsql` PHP extension; does not affect PDO-based PostgreSQL connections.

**Source:** https://github.com/thorsten/phpMyFAQ/security/advisories/GHSA-5hx6-c293-588h

**Actions:**
- Fix: Upgrade phpMyFAQ to version 4.1.7.
- Workaround: Switch from the native `pgsql` extension to the PDO PostgreSQL backend; rate-limit or temporarily block IPs generating excessively broad search patterns.
- Tradeoff: Switching to PDO may require application testing; rate-limiting may affect legitimate users.

#### PostgreSQL — CVE-2026-72775 (CVSS UNSCORED)
**Summary:** n8n before versions 1.123.67, 2.31.5, and 2.32.1 contains a SQL injection vulnerability in the PostgresTrigger node. User-supplied identifier parameters (channel, function, and trigger names) are interpolated into SQL statements without proper escaping. An authenticated user can inject arbitrary SQL executed against the connected PostgreSQL database with the configured credential's privileges, allowing full read and write access.

**Affected Features / Interoperability:** n8n's PostgresTrigger node; impacts n8n workflows that connect to PostgreSQL databases using this trigger.

**Source:** https://github.com/n8n-io/n8n/security/advisories/GHSA-jqwr-vx3p-r266

**Actions:**
- Fix: Upgrade n8n to 1.123.67, 2.31.5, 2.32.1, or later.
- Workaround: Restrict n8n user permissions to only necessary PostgreSQL objects; avoid using PostgresTrigger with untrusted workflow inputs.
- Tradeoff: Restricting permissions may break existing workflows; requires careful permission audit.

#### PostgreSQL — CVE-2026-78699 (CVSS UNSCORED)
**Summary:** The ash-project `ash_postgres` library (versions from 0.25.0 before 2.13.0) has an unchecked return value vulnerability in `AshPostgres.MultiTenancy.rename_tenant/3`. When a tenant is renamed to a name colliding with an existing tenant's schema, PostgreSQL rejects the rename, but the library discards the error result and unconditionally returns `:ok`. The calling action then commits the tenant row with the new name pointing to a different tenant's schema, allowing data access across tenant boundaries.

**Affected Features / Interoperability:** ash_postgres multi-tenancy schema management; impacts Elixir/Phoenix applications using ash_postgres for multi-tenant PostgreSQL deployments.

**Source:** https://github.com/ash-project/ash_postgres/security/advisories/GHSA-6fqq-j9c4-5766

**Actions:**
- Fix: Upgrade ash_postgres to version 2.13.0 or later.
- Workaround: Add database-level constraints to prevent duplicate tenant names; add application-level checks before rename operations; audit tenant data for cross-tenant access anomalies.
- Tradeoff: Database constraints may require schema migration; application checks add logic complexity.

---

#### Oracle Database — CVE-2026-72853 (CVSS 7.6 HIGH)
**Summary:** Budibase before version 3.40.0 contains a SQL injection vulnerability in the Oracle datasource connector's post-write row lookup that fails to escape table names in identifiers. Attackers with write permission on a table with a double-quote character in its name can inject SQL that executes as the datasource's database user, allowing read or modification of arbitrary data in the Oracle database.

**Affected Features / Interoperability:** Budibase Oracle datasource connector's row lookup after write operations; impacts Budibase deployments connecting to Oracle databases.

**Source:** https://github.com/Budibase/budibase/security/advisories/GHSA-xj29-x47g-9w2c

**Actions:**
- Fix: Upgrade Budibase to version 3.40.0.
- Workaround: Restrict write permissions on tables with special characters in names; validate table/column names in application layer before passing to Budibase.
- Tradeoff: Permission restrictions may affect legitimate Budibase application functionality.

---

#### MongoDB — CVE-2026-75852 (CVSS 9.8 CRITICAL)
**Summary:** ArcadeDB versions prior to 26.8.1 fail to enforce SASL authentication on data commands in the MongoDB wire-protocol plugin. Unauthenticated attackers can issue insert, find, update, delete, and create commands against any database by connecting to port 27017 without providing any credentials.

**Affected Features / Interoperability:** ArcadeDB's MongoDB wire-protocol plugin (port 27017); impacts any deployment of ArcadeDB using the MongoDB-compatible API without additional network controls.

**Source:** https://github.com/ArcadeData/arcadedb/security/advisories/GHSA-fq9c-x968-g278

**Actions:**
- Fix: Upgrade ArcadeDB to version 26.8.1 or later.
- Workaround: Enforce network-level access controls (firewall, security groups) to restrict access to port 27017 to trusted IPs only; enable SASL authentication for the MongoDB wire-protocol plugin even if not required for MongoDB compatibility.
- Tradeoff: Network restrictions may affect distributed applications; SASL enforcement may break compatibility with some MongoDB clients expecting no auth.

#### MongoDB — CVE-2026-81532 (CVSS 8.8 HIGH)
**Summary:** Users submitting SQL through applications using the MongoDB Connector for BI ODBC driver can supply a positioned-cursor statement whose cursor name exceeds the size of an internal fixed-length buffer. The name length is not bounded before the driver builds its diagnostic message, overwriting adjacent memory with user-supplied content. This can crash the hosting application and may allow unintended code execution within it.

**Affected Features / Interoperability:** MongoDB Connector for BI ODBC driver; impacts applications using BI tools (Tableau, Power BI, etc.) to query MongoDB via SQL.

**Source:** https://github.com/mongodb/mongo-bi-connector-odbc-driver/releases

**Actions:**
- Fix: Upgrade MongoDB Connector for BI ODBC driver to the version containing the fix (verify with MongoDB security advisories).
- Workaround: Limit ODBC driver access to trusted applications; avoid using positioned cursor statements with untrusted input.
- Tradeoff: May disrupt BI tooling workflows; cursor statement restrictions may limit application functionality.

#### MongoDB — CVE-2026-73618 (CVSS 8.3 HIGH)
**Summary:** Budibase Server before version 3.40.0 has a NoSQL injection vulnerability in the MongoDB query execution endpoint where user-supplied parameters are interpolated into JSON query templates without sanitizing JSON metacharacters. Attackers with query write permission can inject JSON structural characters to alter MongoDB queries, bypassing filters to read, modify, or delete arbitrary documents.

**Affected Features / Interoperability:** Budibase MongoDB datasource query execution; impacts Budibase applications using MongoDB as a datasource.

**Source:** https://github.com/Budibase/budibase/security/advisories/GHSA-qw6m-8fw2-2v64

**Actions:**
- Fix: Upgrade Budibase to version 3.40.0.
- Workaround: Restrict Budibase query write permissions to trusted users; validate query parameter structure in application layer.
- Tradeoff: Permission restrictions may affect application functionality; input validation adds processing overhead.

#### MongoDB — CVE-2026-77586 (CVSS 8.0 HIGH)
**Summary:** In MongoDB Connector for BI, MongoDB object names (collection, field, index names) placed into quoted identifiers in DDL text returned by `SHOW CREATE` statements are not escaped. A user with write permission on a sampled MongoDB collection can choose a name that closes the quoted identifier early, injecting additional SQL text into the generated output. If an operator or automated tool replays that generated statement against a SQL server, the injected text executes with the session's privileges.

**Affected Features / Interoperability:** MongoDB Connector for BI schema sampling and `SHOW CREATE` output; impacts scenarios where BI-generated DDL is replayed on SQL systems.

**Source:** https://www.mongodb.com/docs/bi-connector/current/release-notes/

**Actions:**
- Fix: Upgrade MongoDB Connector for BI to the patched version (verify with MongoDB release notes).
- Workaround: Do not replay `SHOW CREATE` output directly on SQL systems without sanitization; restrict write permissions on MongoDB collections used by BI sampling.
- Tradeoff: Avoiding DDL replay may disrupt automated migration workflows; permission restrictions may limit functionality.

#### MongoDB — CVE-2026-72857 (CVSS 7.7 HIGH)
**Summary:** Budibase before version 3.40.0 fails to redact datasource credentials stored in STRING typed fields, exposing MongoDB connection strings and Firebase private keys in plaintext. Users with table read permissions can retrieve datasource configurations through the read API to obtain live backend database credentials and service account keys.

**Affected Features / Interoperability:** Budibase datasource credential storage and read API; impacts any Budibase deployment with MongoDB or Firebase datasources where credentials are stored in STRING fields.

**Source:** https://github.com/Budibase/budibase/security/advisories/GHSA-6mpp-gfg5-x2vv

**Actions:**
- Fix: Upgrade Budibase to version 3.40.0.
- Workaround: Use environment variables or secrets management instead of STRING-typed credential fields; audit Budibase API access logs for credential retrieval attempts.
- Tradeoff: Migrating credential storage requires application reconfiguration; some workflows may depend on STRING field storage.

#### MongoDB — CVE-2026-81490 (CVSS 7.7 HIGH)
**Summary:** A database user able to create a view in a namespace that MongoDB Connector for BI samples can define a view whose evaluation reliably fails, causing the schema-sampling routine to stop functioning. After retries are exhausted, the sampling logic proceeds without a valid result, leaving the `mongosqld` process running without a usable schema. SQL clients cannot obtain results until an operator removes the view or excludes its namespace from sampling.

**Affected Features / Interoperability:** MongoDB Connector for BI schema sampling; impacts BI deployments where untrusted users can create views in sampled namespaces.

**Source:** https://www.mongodb.com/docs/bi-connector/current/release-notes/

**Actions:**
- Fix: Upgrade MongoDB Connector for BI to the patched version.
- Workaround: Restrict view creation in namespaces used by BI sampling; exclude problematic namespaces from sampling configuration.
- Tradeoff: Restricting view creation may affect legitimate application functionality; namespace exclusions may reduce schema visibility.

#### MongoDB — CVE-2026-81517 (CVSS 7.5 HIGH)
**Summary:** An unauthenticated party able to reach a MongoDB Connector for BI (`mongosqld`) instance can generate enough routine connection log activity to exhaust the storage backing the configured log path. When a log write or rotation fails, the error is unhandled and the shared `mongosqld` process terminates, ending service for all connected SQL clients. The process continues to fail on startup until storage is restored.

**Affected Features / Interoperability:** MongoDB Connector for BI logging subsystem; impacts any mongosqld deployment where network access is not restricted.

**Source:** https://www.mongodb.com/docs/bi-connector/current/release-notes/

**Actions:**
- Fix: Upgrade MongoDB Connector for BI to the patched version.
- Workaround: Restrict network access to mongosqld port; ensure log path has ample storage with monitoring; implement log rotation outside mongosqld.
- Tradeoff: Network restrictions may affect legitimate remote BI clients; additional storage monitoring adds operational overhead.

#### MongoDB — CVE-2026-81518 (CVSS 7.5 HIGH)
**Summary:** When mongosqld is configured with a client certificate authority file, the listener requests a client certificate during TLS handshake but does not require it, so a client presenting no certificate is still accepted. In deployments relying on client certificates as the sole means of user identification, a remote party with network access can establish a session and read data exposed through the connector.

**Affected Features / Interoperability:** MongoDB Connector for BI mutual TLS (mTLS) configuration; impacts deployments using client certificates for authentication.

**Source:** https://www.mongodb.com/docs/bi-connector/current/release-notes/

**Actions:**
- Fix: Upgrade MongoDB Connector for BI to the patched version.
- Workaround: Use alternative authentication mechanisms (e.g., username/password) alongside or instead of client certificates; enforce network-level access controls.
- Tradeoff: Alternative auth mechanisms may require application changes; network controls may affect distributed clients.

#### MongoDB — CVE-2026-81520 (CVSS 7.5 HIGH)
**Summary:** A network-reachable client that has not yet authenticated can hold a MongoDB Connector for BI authentication session open indefinitely by beginning a SASL login exchange and then declining to complete it. There is no overall time bound on the negotiation loop and no read deadline, so each incomplete session retains a worker, connection slot, and backend database connections until process restart. Repeated use can exhaust connection capacity and prevent legitimate users from establishing new sessions.

**Affected Features / Interoperability:** MongoDB Connector for BI SASL authentication handling; impacts any mongosqld deployment with network exposure.

**Source:** https://www.mongodb.com/docs/bi-connector/current/release-notes/

**Actions:**
- Fix: Upgrade MongoDB Connector for BI to the patched version.
- Workaround: Restrict network access to mongosqld; implement external connection rate limiting at the network layer.
- Tradeoff: Network restrictions may affect legitimate remote clients; rate limiting may require infrastructure changes.

#### MongoDB — CVE-2026-73617 (CVSS 7.1 HIGH)
**Summary:** Budibase before version 3.40.0 has a NoSQL injection vulnerability in the MongoDB datasource integration where user-supplied parameters are enriched

## Cloud Security

The single most urgent item in today's digest is **CVE-2026-66384** affecting JFrog Artifactory's Docker cache handling, which CISA has added to the KEV catalog with a remediation deadline of **2026-09-10**. Because this is a confirmed actively-exploited vulnerability with a BOD 26-04 due date, it outranks every other entry below regardless of CVSS. Beyond that, the Kubernetes ecosystem carries the highest theoretical severity this cycle — a CVSS 10.0 unauthenticated SSRF in Kubeflow Pipelines and a CVSS 9.8 unauthenticated arbitrary-file-write in the Dinky Flink dashboard (default Docker image, hardcoded token) — both of which warrant immediate inventory checks. GCP and AWS news surfaces one confirmed healthcare-sector breach tied to AWS infrastructure (Aesto, ~9.5M records), worth noting for threat-modeling context even though it is not a vulnerability in your stack.

### Formal Vulnerabilities

#### JFrog Artifactory — CVE-2026-66384 (CVSS UNSCORED — CISA-confirmed active exploitation) 🔴 ACTIVELY EXPLOITED (CISA KEV)
**Summary:** Improper path-limitation ("path traversal" style) flaw in JFrog Artifactory that lets an **authenticated** user write data outside the intended Docker cache path when specific remote-repository conditions are met. CISA added it to the KEV on 2026-08-27; the required action is to apply vendor mitigations per CISA BOD 26-04 guidance for cloud services (or discontinue use). **KEV remediation due date: 2026-09-10.**

**Affected Features / Interoperability:** Specifically the Docker cache path under remote-repository configurations. Downstream impact includes container image integrity (any out-of-path write could corrupt or poison cached layers), build pipelines that pull cached images from affected repositories, and any CI/CD integration that trusts the Artifactory Docker feed.

**Source:** https://nvd.nist.gov/vuln/detail/CVE-2026-66384

**Actions:**
- Fix: Follow the JFrog advisory linked from the NVD record and apply the vendor-issued patch/upgrade per CISA BOD 26-04.
- Workaround: If a patch cannot be applied by the 2026-09-10 due date, evaluate whether the affected remote-repository/Docker-cache configuration is in use; consider temporarily disabling the affected remote-repo path or restricting the authenticated user population that can write to it.
- Tradeoff: Disabling remote-repo Docker caching will degrade build performance and may break pipelines that depend on remote-layer reuse; restricting write-capable users may impact legitimate developer workflows. Discontinue use only if mitigations are unavailable, per CISA guidance.

#### Kubeflow Pipelines — CVE-2026-54745 (CVSS 10.0 CRITICAL)
**Summary:** Unauthenticated Server-Side Request Forgery (SSRF) in the Kubeflow Pipelines frontend `/apis/v1beta1/_proxy/` (and v2beta1/pipeline variants) routes. The `routePathWithReferer()` helper forwards an attacker-controlled HTTP/HTTPS target to `createProxyMiddleware` with no allowlist and no filtering for loopback, link-local, RFC1918, or in-cluster addresses. The proxy can carry attacker-supplied methods, headers (including `Authorization`, `Cookie`, `X-Forwarded-For`), and POST bodies to internal services and return the upstream response to the unauthenticated client. **No auth is required — only network reachability of the frontend.**

**Affected Features / Interoperability:** Kubeflow Pipelines frontend proxy middleware (`server/proxy-middleware.ts`). Downstream exposure: cloud metadata endpoints (e.g., IMDS for credential theft), the Kubernetes API server, in-cluster services, and any reachable internal HTTP target. This affects ML workflow platforms that integrate with object storage, secrets, and model registries via the same frontend.

**Source:** https://github.com/kubeflow/pipelines/security/advisories/GHSA-gqww-5pj5-8fq7

**Actions:**
- Fix: Upgrade to Kubeflow Pipelines **2.17.0**.
- Workaround: If immediate upgrade is not possible, restrict network ingress to the KFP frontend so the `/apis/*/_proxy/` and `/pipeline/apis/*/_proxy/` paths are not reachable from untrusted networks; enable `ENABLE_AUTHZ=true` and front the UI with an authenticating reverse proxy. Note that the advisory states the route remains outside authz middleware when `ENABLE_AUTHZ=true`, so authz alone does **not** mitigate.
- Tradeoff: Network restrictions may break legitimate integrations with the KFP UI from developer workstations; running a reverse proxy adds operational overhead. There is **no auth-bypass-only workaround** because the route is unauthenticated by design.

#### MCPHub (Docker image) — CVE-2026-79748 (CVSS 9.9 CRITICAL)
**Summary:** MCPHub's `POST /api/servers` and `PUT /api/servers/:name` endpoints accept a server configuration and immediately spawn it via `child_process.spawn`. **Authentication is required, but no authorization check restricts these endpoints to admins**, and there is no allowlist/sanitization on `command`/`args`. Any authenticated non-admin can specify `/bin/sh` (or any binary) with arbitrary arguments, resulting in OS-level code execution as the MCPHub process user (commonly `root` in the published Docker image). Patched in 0.12.15.

**Affected Features / Interoperability:** MCPHub server-registration API and the stdio MCP process orchestrator. The published Docker image, `npx`-hosted deployments, and systemd deployments are all impacted where the process user is privileged. Downstream impact: full container/host compromise given root execution.

**Source:** https://github.com/samanhappy/mcphub/security/advisories/GHSA-mx89-jjx9-gjr8

**Actions:**
- Fix: Upgrade MCPHub to **0.12.15** (or later).
- Workaround: Run MCPHub as a non-root user and front the `/api/servers` endpoints with an admin-only reverse proxy; restrict who can authenticate to MCPHub until the upgrade is applied.
- Tradeoff: Running as non-root may break MCP server processes that expect root (e.g., for low-port binding). Admin-only reverse proxy will prevent non-admins from legitimately registering new MCP servers during the interim.

#### Dinky — CVE-2026-70558 (CVSS 9.8 CRITICAL)
**Summary:** The `POST /download/uploadFromRsByLocal` handler takes a caller-supplied `path` and writes to it with no validation. The route is `@SaIgnore`-marked and excluded from Sa-Token auth, so the only guard is a header equality check against a `dinkyToken` whose **default value `efda1551-7958-4e0f-80a8-dfd107df3e38` is hardcoded in source and shipped to every deployment**. Anyone who can reach Dinky's HTTP port (default 8888) with that token can write arbitrary files as the Dinky service account. The default Docker image has no proxy/auth on 8888 and chmod 777 on `/opt/dinky`, so demonstrated impact includes overwriting `/opt/dinky/config/static/index.html` (XSS to admin browsers) and writing `/opt/dinky/org/dinky/Dinky.class` (code execution as the service account at next JVM start via the classpath-shadow launched by `script/bin/auto.sh`). Writes run as uid 9999 (flink), not root, so `/etc`, `/root`, `/home`, `/usr` are refused. **Affects Dinky v1.2.5 (current release) and the development branch — code is byte-identical.** No patch is currently indicated in the advisory data.

**Affected Features / Interoperability:** Dinky Flink dashboard's `/download/**` HTTP handlers and the JVM classpath loaded by `auto.sh`. Downstream impact: admin-browser XSS, arbitrary code execution inside the Dinky container on next restart, and any Flink job artifacts living under `/opt/dinky`.

**Source:** https://github.com/DataLinkDC/dinky/security/advisories/GHSA-2p66-w3p3-5226

**Actions:**
- Fix: No patched version is indicated in the supplied data; track the GitHub advisory for an upstream fix.
- Workaround: Block external access to Dinky port 8888 at the network layer (firewall, security group, ingress controller); rotate `dinkyToken` from the default immediately (note: the source still contains the default, so this only protects deployments that have already changed it); run the container as a non-writable user and remove `chmod 777` from `/opt/dinky`.
- Tradeoff: Hard-coded-token deployments cannot mitigate via token rotation alone; network restriction may block legitimate analyst/developer access, and a non-writable filesystem may break Dinky upgrades that rely on self-write paths. **No significant tradeoff** for the network restriction beyond access friction.

#### MapFish Print — CVE-2026-55848 (CVSS 8.6 HIGH)
**Summary:** `mapfish-print` accepts an attacker-controlled GML layer URL in requests to `/api/print3/print` and fetches XML parsed by `core/src/main/java/org/mapfish/print/map/geotools/GmlLayer.java` **without disabling external entities and external DTDs**. A remote XML/DTD can expand a local file entity, exposing its contents via the GML parsing and error path, allowing unauthenticated attackers to read files such as OS account data, **Kubernetes service-account tokens**, and certificates. Swapping the entity target to an internal HTTP endpoint also enables SSRF. Fixed in versions 3.28.30, 3.30.32, 3.31.24, 3.33.16, and 4.0.5.

**Affected Features / Interoperability:** MapFish Print `/api/print3/print` endpoint and the GmlLayer parser. Downstream: any cluster or service-account-token mounted into the MapFish pod is at risk of exfiltration; any reachable internal HTTP service is at risk of SSRF.

**Source:** https://github.com/mapfish/mapfish-print/security (referenced commits linked from advisory)

**Actions:**
- Fix: Upgrade to one of **3.28.30 / 3.30.32 / 3.31.24 / 3.33.16 / 4.0.5** depending on your release line.
- Workaround: Restrict network access to the `/api/print3/print` endpoint and disable GML layer URL acceptance in config if your deployment supports it; consider denying outbound egress from the MapFish pod.
- Tradeoff: Disabling GML layers will break legitimate templated map workflows that rely on external GML data sources; egress restrictions will also block those sources.

#### Grafana Alloy — CVE-2026-75889 (CVSS 7.7 HIGH)
**Summary:** Grafana Alloy's `prometheus.operator.servicemonitors` component lets a user who can create/modify `ServiceMonitor` resources in a watched namespace specify an arbitrary local file via `bearerTokenFile`. Alloy reads the file and sends its contents as a bearer token to an attacker-controlled scrape endpoint, **disclosing files readable by the Alloy process — including its projected Kubernetes service-account token**. Exploitation requires ServiceMonitor write access and lower privileges than Alloy's service account.

**Affected Features / Interoperability:** Grafana Alloy's ServiceMonitor controller. Downstream: Kubernetes service-account tokens and any other files readable by Alloy are exfiltratable to an attacker-controlled Prometheus endpoint; compromise of the SA enables further cluster action depending on RBAC.

**Source:** https://grafana.com/security/security-advisories/cve-2026-75889

**Actions:**
- Fix: Follow the Grafana security advisory for the patched Alloy version.
- Workaround: Restrict who can create/modify `ServiceMonitor` resources (use RBAC on `monitoring.coreos.com/servicemonitors`) until patched; consider denying Alloy's outbound egress to non-allowlisted hosts.
- Tradeoff: RBAC tightening will block legitimate tenants from adding ServiceMonitors for their workloads; egress restrictions require maintaining an allowlist of scrape targets.

#### Kyverno — CVE-2025-15613 (CVSS 6.5 MEDIUM)
**Summary:** SSRF in Kyverno's Service Call functionality (before v1.13.4). An attacker with permission to create Kyverno `(Cluster)Policy` resources can specify an external URL in a policy's `apiCall`/`service` configuration; although Service Call is documented for in-cluster services, it also resolves external addresses. Policy context data — **including Kubernetes resource contents such as Secrets** — is sent in these requests, allowing exfiltration of sensitive cluster data.

**Affected Features / Interoperability:** Kyverno policy `apiCall`/`service` blocks; any Secret or ConfigMap content that flows through policy context evaluation is at risk of exfiltration. Downstream integrations that consume Kyverno policy evaluation results may also be impacted.

**Source:** https://github.com/kyverno/kyverno/security/advisories/GHSA-459x-q9hg-4gpq

**Actions:**
- Fix: Upgrade to **Kyverno v1.13.4** or later.
- Workaround: Restrict who can create `(Cluster)Policy` resources to trusted admins until the upgrade is applied.
- Tradeoff: Restricting policy-creation rights will delay legitimate policy authoring by developer teams; no significant tradeoff beyond process friction.

#### Google Cloud BigQuery Data Transfer Service — CVE-2026-12717 (CVSS UNSCORED)
**Summary:** Improper input validation in the CData JDBC driver integration in Google Cloud BigQuery Data Transfer Service allows an **authenticated** attacker to achieve remote code execution in the connector container and escalate privileges in the tenant project using crafted JDBC connection-string parameters. **Patched on 1 May 2026; no customer action is needed per Google.**

**Affected Features / Interoperability:** BigQuery DTS CData JDBC connector container; downstream tenant-project privilege escalation if the connector is exploited before the patch window.

**Source:** https://docs.cloud.google.com/support/bulletins#gcp-2026-056

**Actions:**
- Fix: Already patched by Google on 2026-05-01; verify your connectors were auto-updated.
- Workaround: None required per vendor; restrict who can configure JDBC connection-string parameters in BigQuery DTS as defense-in-depth.
- Tradeoff: **No significant tradeoff.**

#### Google SecOps (Chronicle SOAR) — CVE-2026-15587 (CVSS UNSCORED)
**Summary:** Improper privilege management in Google SecOps (Chronicle SOAR) prior to 6.3.85 allows an **authenticated** attacker to escalate privileges to system-level administrative access using a crafted internal authentication header. **Patched in 6.3.85; no customer action is needed per Google.**

**Affected Features / Interoperability:** Chronicle SOAR internal authentication header handling and privilege escalation paths. Downstream: SOAR playbooks, integrations, and any connected security tooling that trust the SOAR's identity model.

**Source:** https://docs.cloud.google.com/chronicle/docs/soar/release-notes#May_23_2026

**Actions:**
- Fix: Already patched in 6.3.85; confirm tenant is on or above this version.
- Workaround: None required per vendor.
- Tradeoff: **No significant tradeoff.**

#### Google SecOps (Chronicle SOAR) — CVE-2026-15623 (CVSS UNSCORED)
**Summary:** SQL injection in a legacy dashboard widget API in Chronicle SOAR prior to 6.3.85 allows an **authenticated** attacker to execute blind SQL queries using a crafted request parameter. **Patched in 6.3.85; no customer action is needed per Google.**

**Affected Features / Interoperability:** Legacy dashboard widget API in SOAR; SQL backend and any data exposed through that backend.

**Source:** https://docs.cloud.google.com/chronicle/docs/soar/release-notes#May_23_2026

**Actions:**
- Fix: Already patched in 6.3.85.
- Workaround: None required per vendor.
- Tradeoff: **No significant tradeoff.**

#### Google Cloud Build — CVE-2026-19410 (CVSS UNSCORED)
**Summary:** Incorrect authorization in the GitHub Trigger Comment Control in Cloud Build prior to 2026-06-24 allows a remote attacker to execute unreviewed code in the build environment by suppressing the webhook review flow. **Patched on 24 June 2026; no customer action is needed per Google.**

**Affected Features / Interoperability:** Cloud Build GitHub trigger comment control and the build execution environment. Downstream: any artifact produced by suppressed builds, and any deployment pipeline downstream of Cloud Build that trusts the artifact.

**Source:** https://docs.cloud.google.com/build/docs/release-notes#August_24_2026

**Actions:**
- Fix: Already patched on 2026-06-24; confirm Cloud Build is auto-updated.
- Workaround: None required per vendor.
- Tradeoff: **No significant tradeoff.**

#### Google Cloud Vertex AI Search for Commerce — CVE-2026-19485 (CVSS UNSCORED)
**Summary:** Predictable resource name vulnerability in BigQuery Import Staging in Vertex AI Search for Commerce prior to 2026-04-27. An attacker who knows the victim's project number can obtain read/write access to staged data and error logs by guessing predictable bucket names. **Patched; no customer action is needed per Google.**

**Affected Features / Interoperability:** BigQuery import staging buckets for Vertex AI Search for Commerce; error logs and staged data.

**Source:** https://unit42.paloaltonetworks.com/hijacking-vertex-ai-model/

**Actions:**
- Fix: Already patched by Google.
- Workaround: None required per vendor.
- Tradeoff: **No significant tradeoff.**

### News & Community Mentions

*Unverified chatter — not authoritative for patch decisions.*

- **AWS:** Aesto Health Data Breach impacted ~9.5M people; hackers stole personal and health data from the healthcare technology company's AWS infrastructure — relevant threat-modeling signal for healthcare AWS tenants. *(SecurityWeek: https://www.securityweek.com/9-5-million-impacted-by-aesto-health-data-breach/)*
- **AWS:** Threat actors are actively exploiting critical flaws in Langflow and Ruby on Rails for credential-probing and C2 activity, per VulnCheck — broadly relevant for any internet-exposed Rails or Langflow workloads. *(The Hacker News: https://thehackernews.com/2026/09/attackers-exploit-critical-langflow-and.html)*
- **AWS / Azure:** Microsoft and AWS are rolling out multicloud private 100 Gbps interconnect services — operational relevance for hybrid AWS/Azure architecture planning. *(The Register: https://www.theregister.com/off-prem/2026/09/01/microsoft-and-aws-build-the-multicloud-bridge-they-said-customers-barely-needed/5293614)*
- **Kubernetes:** Broadcom launched Trusted Artifact Service for Spring Framework and an AgentMinder/Avi/vDefend update set pitched at agentic AI workload security — operational relevance for Spring- and Kubernetes-based pipelines. *(DevOps.com via news.google.com; storagereview.com via news.google.com)*
- **Microsoft Azure:** Azure announced "Multicloud Interconnect for AWS" — mirrors the AWS-side announcement above; relevant for cross-cloud networking posture. *(Microsoft Azure via news.google.com: https://news.google.com/rss/articles/CBMikwFBVV95cUxPQWl1SVNnd0Y2UW9uUjdEYWRqcFlsVDZ3OUlfVDVUTWc1UkpiNnN2d3FHRF9pMDd1SzI2dzhIeHZHczg3WXRHZjgzUG8yMlNPVmEybDJ1dno5UU9YVEszWEF5MnNwVFRNUkxfaTZub3dYY0Y1UFljaklmTC0yaWNMakxYYmFJRjBFUjNjbmt0QnN3cEk)*
- **Google Cloud Platform:** A write-up on eliminating long-lived credentials in GCP via Workload Identity Federation — defensible-architecture reading for service-account posture. *(infoq.com via news.google.com)*