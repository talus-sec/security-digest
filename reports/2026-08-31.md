# Security Digest — 2026-08-31

## Database

   [One-paragraph executive summary highlighting the single most urgent item]

   ### Formal Vulnerabilities

   [For each system with entries, format as specified]

   ### News & Community Mentions

   [Bullet list if there's data]
   ```

   Let's construct the executive summary. It should be one paragraph, highlighting the single most urgent item. The most urgent is the Microsoft SQL Server CVE-2019-1068 with CISA KEV confirmed. I'll write something like: "This digest covers critical vulnerabilities across MySQL, PostgreSQL, and Microsoft SQL Server environments, with the highest priority given to a CISA KEV-confirmed remote code execution flaw in Microsoft SQL Server (CVE-2019-1068) that is actively exploited in the wild and carries a due date of 2026-08-29, outweighing all high-CVSS entries. Additional findings include sensitive information exposure in WordPress backup plugins, SQL injection and credential leakage in MariaDB connectors, and authorization bypass in a student management system, along with a PostgreSQL SQL injection flaw in WAPT Server. Immediate patching and mitigation are urged for the KEV entry and all high-severity issues."

   Wait, the executive summary should be one paragraph, highlighting the single most urgent item. I'll make sure it's a single paragraph.

   Now, let's format the Formal Vulnerabilities section. I need to group by system. The order of systems? The JSON has MySQL, PostgreSQL, Microsoft SQL Server. I'll follow that order.

   For each system, I list entries in the order given (KEV first, then CVSS descending). For MySQL, the entries are already in CVSS descending order (7.5, 6.5, 6.3, 5.9), no KEV. For PostgreSQL, 1 entry. For MSSQL, 1 KEV entry.

   I need to output each entry as:
   `#### [System Name] — [CVE ID] (CVSS [score or "UNSCORED"] [severity]) [add "🔴 ACTIVELY EXPLOITED (CISA KEV)" if confirmed_exploited_by_cisa_kev is true]`

   Then the subsections: **Summary**, **Affected Features / Interoperability**, **Source**, **Actions**.

   Let's do MSSQL first because it has the KEV tag. The format: `#### Microsoft SQL Server — CVE-2019-1068 (CVSS UNSCORED) 🔴 ACTIVELY EXPLOITED (CISA KEV)`? Wait, the tag is added after the parenthesis. The instruction: `#### [System Name] — [CVE ID] (CVSS [score or "UNSCORED"] [severity]) [add "🔴 ACTIVELY EXPLOITED (CISA KEV)" if confirmed_exploited_by_cisa_kev is true]`. So the tag is outside the parenthesis. So it would be:
   `#### Microsoft SQL Server — CVE-2019-1068 (CVSS UNSCORED) 🔴 ACTIVELY EXPLOITED (CISA KEV)`
   But what about the severity inside the parenthesis? The format has `[severity]` inside. For this entry, the data's cvss_severity is "UNSCORED — CISA-confirmed active exploitation". I need to decide what to put inside. I'll put "UNSCORED" as the severity, and the tag handles the KEV part. But the format says `(CVSS [score or "UNSCORED"] [severity])`. If I put "UNSCORED" as severity, it's `(CVSS UNSCORED UNSCORED)`? No, I'll just put `(CVSS UNSCORED)` and then the tag. But the format expects a severity inside. Maybe I should put the severity as given? The user said "Do not fabricate...". I'll output `(CVSS UNSCORED)` and then the tag. I think that's the intent: the severity slot is for like HIGH, MEDIUM, and UNSCORED is a placeholder when there's no score. I'll output `(CVSS UNSCORED)` and then `🔴 ACTIVELY EXPLOITED (CISA KEV)`.

   Actually, re-reading: "#### [System Name] — [CVE ID] (CVSS [score or "UNSCORED"] [severity]) [add "🔴 ACTIVELY EXPLOITED (CISA KEV)" if confirmed_exploited_by_cisa_kev is true]". The `[severity]` is part of the parenthesis. For entries with a CVSS score, it would be like `(CVSS 7.5 HIGH)`. For this entry, score is null, so "UNSCORED", and severity... The data has "UNSCORED — CISA-confirmed active exploitation". I could output `(CVSS UNSCORED)` and then the tag, ignoring the severity inside, or I could output `(CVSS UNSCORED UNSCORED)` no. I'll output `(CVSS UNSCORED)` and then the tag. The severity inside will be "UNSCORED" and the tag adds the KEV info. That seems consistent.

   Wait, maybe the severity inside should be the string from the data? But the format says `[severity]`, which typically is HIGH/MEDIUM/LOW. The data's severity is "UNSCORED — CISA-confirmed active exploitation".

User Safety: safe